# 小程序接口联调进度

## 目标

本文件是小程序对接后端接口的执行表。根目录负责跨项目同步，本文件只记录小程序侧的鉴权模式、字段归一、页面映射、验证进度、剩余风险和 HANDOFF，不复制 Feishu DTO 全量字段。

## 当前批次

| 批次 | 辅助 commit | 来源 | 小程序结论 |
| --- | --- | --- | --- |
| `BE-2026-05-19-feishu-bff-base` | `ff17e45` | Feishu 主文档：`backend-server/docs/hellokitty-park-feishu-api-doc.md`；OpenAPI：`backend-server/docs/swagger/hellokitty-park-bff-service-openapi.json` | Feishu 主文档 13 个 BFF 对外入口已在小程序 service/request 层落地；小程序不直连 promotion/pay/admin-config/xxl-job 内部服务接口 |
| `BE-2026-05-28-purchase-cms` | `ff17e45` | 后续变更：`backend-server/release-records/uploads/2026-05-27-purchase-module/purchase-module-api.md` | 购票列表和购票页资源位已接入票务页面数据服务；最新鉴权由 `BE-2026-06-07-mini-program-ads` 覆盖为登录态 GET；本轮已删除票务预定列表和资源位运行时静态降级，接口失败进入异常态、空数组进入空态 |
| `BE-2026-05-28-crm-uat` | `b40c8ed` | uat 后续变更：`backend-server/release-records/uploads/2026-05-28-crm-module/crm-module-api.md`、`backend-server/release-records/uploads/2026-05-29-mini-program-p0-p1/mini-program-p0-p1-api-plan.md` | CRM 会员中心、资料、地址、会员码、老会员绑定、领券中心和兑换专区已落小程序真实 service 层；已接入页面不再失败回旧本地会员、券或兑换数据 |
| `BE-2026-05-30-order-checkout-uat` | `b40c8ed` | uat 后续变更：`backend-server/release-records/uploads/2026-05-30-order-checkout/order-checkout-api.md` | 首版订单提交、订单详情、订单列表 BFF service 已落地；酒店链路已由 `BE-2026-06-09-hotel-order-bff` 覆盖为统一订单真实链路，订单列表/详情/继续支付/取消/申请退款已切真实 BFF；票务确认单、创建订单和支付已切统一订单真实接口，商品详情/日历价量/票码展示仍待后端 BFF |
| `BE-2026-06-03-wechat-login-fix-uat` | `b40c8ed` | uat 后续变更：`backend-server/release-records/uploads/2026-06-03-wechat-login-fix/README.md` | 后端修复微信 `jscode2session` text/plain 响应导致的登录 500；小程序沿用现有授权路径，待微信开发工具用真实 code 复测 |
| `BE-2026-06-03-login-content-type-token-uat` | `37383fb` | uat 后续变更：`backend-server/release-records/2026-06-03-login-content-type-fix.md`；commit-log `v0.1.9`/`v0.1.10` | 后端将登录请求体/媒体类型错误转为 `400 REQUEST_BODY_INVALID` / `415 UNSUPPORTED_MEDIA_TYPE`，并将 JWT/Redis 登录态签发异常转为明确业务错误；小程序授权请求已显式传 `content-type: application/json`，本批次无需改代码，待真实 code 复测 |
| `BE-2026-06-06-cms-auth-tighten-uat` | `c06ca8f` | uat 最新提交：`fix(bff): 收紧CMS资源接口鉴权`；安全配置：`hellokitty-park-bff-service/src/main/resources/application.yml` | 后端移除 `/api/bff/cms/**` 公开白名单，CMS 资源位直连 BFF 需要登录 token；最新鉴权由 `936aa38/00486d4` 覆盖为除登录/刷新外所有 BFF 业务接口均需小程序 `Authorization`；小程序购票页资源位失败不再回退旧图片或旧内容 |
| `BE-2026-06-07-mini-program-ads` | `6fbe8f8`、`31cabcb`、`936aa38`、`00486d4` | 后续变更：`backend-server/release-records/frontend-api/changes/2026-06-07-mini-program-ads.md`、`backend-server/release-records/frontend-api/changes/2026-06-07-bff-mini-program-auth-restore.md`、`backend-server/release-records/frontend-api/mini-program-api.md` | 后端新增小程序广告聚合和单广告详情接口，并在 2026-06-07 恢复小程序 BFF 业务接口鉴权；首页广告、购票列表和购票资源位 GET 均需先完成小程序授权并携带 `Authorization`，不需要签名；广告字段补齐 `id/pageId/slotId/fieldConfig/adName/backgroundImage/materialImage/iconImage/jumpPath` 等前端字段；广告真实接口链路不允许失败回旧本地内容 |
| `BE-2026-06-08-mini-program-slot-ads` | `b77d398` | 后续变更：`backend-server/release-records/frontend-api/changes/2026-06-08-mini-program-slot-ads.md` | 后端新增 `/api/bff/content/mini-program/slots/{slotCode}/ads`，首页“查看更多”列表页可按资源位编码直查可见广告；小程序列表页已移除旧本地 mock，空数组进入空态，接口错误进入异常态 |
| `BE-2026-06-08-member-auth-status` | `30d37b1`、`348945c`、`29efdf7`、`5473fe3`、`da1b529` | 后续变更：`backend-server/release-records/frontend-api/changes/2026-06-08-mini-program-phone-authorize.md`、`2026-06-08-bff-member-identity-hardening.md`、`2026-06-08-bff-member-status-upload.md`、`2026-06-08-bff-crm-entry-rename.md`、`mini-program-api.md` | 小程序登录只换 BFF token，会员登录态统一由 `GET /api/bff/auth/member/status` 的 `memberLoggedIn/memberInfo.phone` 判断；启动默认 `login -> member/status` 缓存 MobX `memberInfo`，受保护入口只读 `rootStore.isLoggedIn` 做弹窗拦截，不主动重复 login/memberStatus；手机号授权后和资料更新后用当前 session 刷新 `member/status`；登录弹窗只保留微信手机号授权；会员资料/会员码/会员中心链路不再失败回旧 mock；CRM 入口路径从 `/crm/p1/**` 改为 `/crm/entries/**`；前端不再传/读 `memberNo/openId/userId/channelOpenId/payerId` |
| `BE-2026-06-09-hotel-order-bff` | `3fe0c1a` | `backend-server/release-records/frontend-api/mini-program-api.md`、`backend-server/release-records/frontend-api/changes/2026-06-09-bff-hotel-catalog-api.md`、`2026-06-09-bff-unified-order-api.md`、`2026-06-09-bff-order-scene-enum.md` | 小程序酒店首页、房型列表、房型详情、确认单、创建订单、支付和订单中心列表/详情已切真实 BFF；酒店分包运行时 mock 数据文件已删除，订单中心核心列表/详情不再读取本地订单，接口失败、配置缺失或无富文本按异常态/空态处理 |
| `BE-2026-06-16-ticket-closure-recovery` | `abbe80b`；2026-06-18 11:52 后端 `origin/uat` 最新为 `c379081` | `backend-server/release-records/frontend-api/changes/2026-06-10-ticket-p0-admin-bff.md`、`2026-06-13-mini-program-ticket-display-status.md`、`2026-06-13-ticket-calendar-draft-empty.md`、`2026-06-13-ticket-order-payment-bypass.md`、`2026-06-13-bff-order-payment-signature-compat.md`、`2026-06-14-p0-coupon-ticket-kcoin-uat.md`、`2026-06-17-ticket-zhiyoubao-goods-code.md`、`2026-06-17-ticket-order-status-polling-check.md`、`2026-06-18-admin-ticket-zhiyoubao-publish-guard.md` | 门票预订页已从旧 `purchase/menus` 切到 `/api/bff/tickets/products`、`/products/{productCode}/calendar` 和 `/quote`；快速通、草稿/待审核待上线、已发布库存可售、商品规则弹窗、0 元票和按 SKU 实名字段提交、空票种空态已按真实接口收口；按后端订单兼容契约，`/api/bff/orders/confirm`、`POST /api/bff/orders`、`POST /api/bff/orders/{orderNo}/pay` 已改为登录态 Bearer，不再强制 HMAC 签名。小程序创建订单已同步首位实名游客证件到 `context.certificateNo`，兼容当前 order-service 只读 context 证件号的出票构造。最新运行态商品、日历、报价和统一订单确认单均 200；创建探针 traceId=`ticket-closure-mqiyobbo-05` 已触发智游宝发码成功，但 BFF 返回 `INTERNAL_ERROR` 且本地订单事务回滚，DB 只读复核确认 `park_order_fulfillment.fulfillment_status` 实际约束仍是旧枚举，不允许后端写入 `WAIT_USE`。订单详情已新增票务凭证页内 3 秒后台静默轮询，失败不弹窗、不展示 loading，状态指纹不变不触发页面重渲染，探针响应不直接渲染，发现变化后再走正常订单详情刷新入口更新页面；后端 `a81410c` 文档确认轮询仍使用既有 `GET /api/bff/orders/{orderNo}`，智游宝回调会更新 `ticketStatus/usedNum/totalNum` 并推进订单 `WAIT_USE/PART_USED/USED/FULFILLED`，真实核销刷新待后端修复履约表约束后端到端复测 |
| `BE-2026-06-18-ticket-voucher-closure` | `12ac189` / `6ecc3be` / `c79a546` / `cf7e4b5` / `d92be1e` / `2e44302` / `94d87f6` / `b4fb3f7` / `b2a8a37` / `f56a059` | `backend-server/release-records/frontend-api/changes/2026-06-18-ticket-zhiyoubao-img-code-image.md`、`backend-server/release-records/frontend-api/changes/2026-06-18-ticket-order-real-wechat-pay.md`、`backend-server/release-records/frontend-api/mini-program-api.md`、后端 `V42__extend_park_order_fulfillment_status.sql`、`BffOrderController`/`UnifiedOrderService` diff、根探针 `/tmp/hkitty-ticket-closure/probe-strict-after-backend-6ecc3be.json` | 后端已把 `park_order_fulfillment.fulfillment_status` 扩展到 `WAIT_USE/PART_USED/USED/FULFILLED` 等票务履约状态，并把智游宝二维码查询返回的 `img` 映射到既有 `ticketVouchers[].codeImage/qrImage`；`12ac189` 进一步把裸 base64 归一为可渲染 data URI，`6ecc3be`/`c79a546` 修复旧直出票退款和退款终态保护，`cf7e4b5` 让内部 `check-status` 主动查询后同步 order-service 履约，`d92be1e` 撤回创建后 `BYPASS_` 免支付出票并恢复门票真实微信支付，`2e44302` 后 UAT 运行态曾返回门票 `/pay` 微信预支付参数，16:44 AppID 探针证明 `paymentParams.appId=wx72b9e08ce45d3e79` 正确；`94d87f6/b4fb3f7` 已补后端 AppID 字段透传。21:22 在 `origin/uat@f56a059` 下复跑，`e3b4d3c/54fcd24/6e3623a/248198f/f56a059` 只改会员查询性能、折扣券同源字段和多业态券同源场景，不触达票务源码，`products/calendar/quote/confirm/create` 已恢复 200，订单 `TKT20260619212212B6FB3624` 的 `/pay` traceId=`ticket-after-f56a059-20260619212212-06` 仍返回 `/api/pay/prepay failed`。历史严格探针 traceId=`ticket-closure-mqj7yaw3-01..06` 已证明旧免支付链路可进入 `WAIT_USE` 并返回凭证；历史订单 `TKT202606181516448D71CD09` 标准化回调后详情回读 `USED/FULFILLED/usedNum=1`；小程序 BFF 订单详情不会自动调用内部 `check-status`。当前小程序已移除 `paymentSkipped` 依赖，且前端 AppID 传参已按微信运行态 API 收口；当前等待后端恢复 pay-service 真实预下单稳定，之后再验真实微信支付成功后出票、真实支付退款样本、后台票码实例和核销流水同步。 |
| `BE-2026-06-19-ticket-fast-pass-local-verifier` | `79c14fc` / `18b68f0` / `fde97b8` | `backend-server/release-records/frontend-api/changes/2026-06-19-ticket-fast-pass-local-verifier.md`、`backend-server/release-records/frontend-api/api-change-log.md`、`backend-server/release-records/frontend-api/mini-program-api.md`、`BffTicketVerificationController`、`TicketVerificationController`、`UnifiedOrderService#localTicketVoucherPayload/#ticketVoucher` | 后端新增快速通本地自产码和商米票码核销链路：`fastPass` 支付成功后由 order-service 本地生成 `ticketVouchers[].ticketCode/voucherCode/qrCodePayload/codeImage/ticketStatus/usedNum/totalNum`，不再依赖智游宝取码；新增公开核销接口 `POST/GET /api/bff/verifier/ticket-verifications/**` 和 `POST/GET /api/admin-config/ticket-verifications/**`，核销后同步 `admin_ticket_instance`、操作日志、`park_order_fulfillment` 和 `park_order`。小程序端不新增核销请求，只继续读取 `GET /api/bff/orders/{orderNo}`；本轮已把订单详情票码映射补齐 `qrCodePayload`，并把 `FULFILLED/COMPLETED/SUCCESS` 票码状态展示为“已核销”、订单列表展示 `PART_USED` 为“部分使用”。`18b68f0` 已回填 UAT 发布结果，BFF/admin-config 公开记录查询均实测 `200 OK` 空列表；`fde97b8` 仅新增 6 月 18 日后台/小程序复核文档，不新增票务运行时代码或接口。仍需真实微信支付成功样本和商米/后台核销样本复验业务状态一致性。 |
| `BE-2026-06-16-coupon-bff-required` | `abbe80b`；2026-06-18 12:36 后端 `origin/uat` 最新为 `e3138d9` | `backend-server/release-records/frontend-api/changes/2026-06-13-bff-member-coupon-assets.md`、`backend-server/release-records/frontend-api/changes/2026-06-17-bff-coupon-kcoin-same-source.md`、`backend-server/release-records/frontend-api/changes/2026-06-17-bff-available-coupon-filters.md`、`backend-server/release-records/frontend-api/changes/2026-06-18-bff-coupon-refund-return-idempotency.md`、`backend-server/release-records/frontend-api/changes/2026-06-18-bff-mall-cart-real-api.md`、`backend-server/release-records/frontend-api/changes/2026-06-18-bff-mall-order-hotel-coupon-contract.md`、`backend-server/release-records/frontend-api/changes/2026-06-18-bff-mall-empty-state-hardening.md`、`backend-server/release-records/frontend-api/changes/2026-06-18-bff-mini-program-mock-disabled.md`、`backend-server/release-records/frontend-api/mini-program-api.md`、后端 BFF/Member/Promotion/Admin/Order/Mall/Promotion 源码核验；`e3138d9` 仅改 order-service 深大默认场所/仓库配置，无券或商城 BFF 契约变化 | 后端已存在我的券、券包、领券、可用券、锁券、核销、释放、退款返还和 K 币接口；`claim` 当前真实入参为 `templateNo`。`c21e2cd/5d50789` 已从源码层补齐 K 币兑换、后台发券和券包赠送同步同一 `couponNo` 到 `promotion_member_coupon`，`member/coupons` 已补分页、来源和返还状态字段，`available` 已补可用性响应字段；`27c68ed` 已补 `available` 商品/SKU/日期入参和目标过滤，`ad09fb4` 已补订单 confirm / promotion quote 候选目标校验，`63e88b5` 已补退款返还幂等，`1b3ae92/ee74bd6` 已补商城购物车 BFF 源码和 release，`beb169c/c379081` 已补商城展示与购物车读接口空态 200。小程序 service 已按新版字段适配；当前仍缺目标 `couponNo` 严格探针、管理端券模板同步 `target_ids`、真实已核销券退款返还样本验收，以及后台配置真实商城商品后的加购/统一订单/用券写入验收。字段级必补清单维护在 `admin-frontend/docs/codex/admin-api-requirements/mp-bff-requirements/coupon-bff-required-interfaces-2026-06-16.md`、`coupon-bff-required-interfaces-2026-06-17.md`、`coupon-bff-required-interfaces-2026-06-18.md`，页面映射见 `route-api-map.md` |

| `BE-2026-06-19-coupon-resume` | `a0341e2`；远端 admin `feature/admin-business-platform@f91e2e5` | 后端 `origin/uat`、`backend-server/release-records/frontend-api/**`、admin 小程序 BFF 必补目录、`yarn probe:coupon-closure`、CRM 入口扫描 | `a561839` 已补 CRM 入口动作字段源码和券/K 币错误透传，`bcee91a/fb5082c` 收紧管理端会员发券小程序身份校验，`a0341e2` 在 promotion 读取会员券、可用券和退款返还前自动回填历史 CRM 会员券同源资产；只读券探针 `coupon-closure-mqktfl28-01..04` 四个接口 200，但当前会员仍无券、无券包、K 币余额 0，不能证明后台发券或 K 币兑换券闭环；小程序前端侧已修正领券中心 K 币 tab 数据源，从 `crm/entries/coupons` 改为真实 `crm/entries/exchanges` |

### 票务闭环最新覆盖结论

- 2026-06-19 13:25 复拉后端到 `origin/uat@2e44302 fix(pay): 接入微信真实退款`；在 `cf7e4b5` 内部主动查询同步和 `d92be1e` 恢复真实微信支付基础上，最新运行态探针已证明 `POST /api/bff/orders` 创建待支付门票订单和 `POST /api/bff/orders/{orderNo}/pay` 预支付均可用。订单 `TKT202606191325080E335D16` 创建为 `PENDING_PAYMENT`，traceId=`ticket-closure-mqkhitex-07`；`/pay` traceId=`ticket-closure-mqkhitex-08` 返回 `success=true`、`orderStatus=PAYING`、`prepayPayNo=PAY202606191325081664ffd075f9`、`hasPaymentParams=true`；付款前详情 traceId=`ticket-closure-mqkhitex-09` 仍无券码，探针单已通过 `ticket-pay-cleanup-mqkhjaem` 取消。小程序已移除 `paymentSkipped` 依赖，创建后必须调用 `/pay` 并在微信支付成功后刷新详情读取 `ticketVouchers[]`。历史严格探针、旧 `BYPASS_` 退款和退款终态保护证据保留为后端历史回归样本；当前剩余缺口是真实微信支付成功后出票复验、真实支付退款样本、后台票码实例和核销流水未同步，以及后端是否把内部主动同步接入定时任务或后台触发链路。
- 2026-06-19 16:05 复拉后端到 `origin/uat@543414e feat(multi): 新增快速通自产码与商米票码核销链路`。本批把快速通从智游宝出票分支切到本地自产票码，并新增商米/后台票码核销接口；小程序订单详情仍只读既有 `GET /api/bff/orders/{orderNo}`，不新增前端核销请求。本轮小程序已补 `qrCodePayload` 读取、`FULFILLED/COMPLETED/SUCCESS` 票码状态中文化、订单列表 `PART_USED` 文案和出票判断状态集。该时间点 release 仍标注未发布，发布状态已被 16:22 的 `18b68f0` 复核覆盖；当前仍需用真实微信支付样本验证 `ticketVouchers[]`，再用商米/后台核销样本验证 3 秒静默轮询能把订单从 `WAIT_USE/PART_USED` 更新到 `USED/FULFILLED`。
- 2026-06-19 16:09 运行态复核：`node --check scripts/probe-ticket-closure.mjs` 通过；默认探针 `ticket-closure-mqkne1jg-03..06` 商品、日历、报价和确认单继续 200。显式创建探针 `ticket-closure-mqknehsb-01..07` 创建订单 `TKT20260619160942DB0D7E2D`，`/pay` 返回 `PAYING/prepayPayNo=PAY202606191609425594b1d98a92/hasPaymentParams=true`，付款前详情无券码，探针单已签名取消并由 `ticket-pay-cleanup-mqknehsb-verify` 回读 `CANCELED`。这说明新提交未破坏预支付；支付成功后本地出票、真实支付退款和核销同步仍必须等真实微信支付/商米样本复验。
- 2026-06-19 16:22 再次主动同步后端，`origin/uat` 已到 `18b68f0 docs(frontend-api): 回填快速通自产码UAT发布结果`。后端文档确认 `79c14fc` 已发布 `order-service/admin-config/bff`，BFF 和 admin-config 公开 `ticket-verifications` 记录查询均实测 `200 OK` 空列表。发布后默认探针 `ticket-closure-mqknr74r-01..04` 继续通过；显式创建探针 `ticket-closure-mqknri5q-01..07` 创建订单 `TKT202606191619491135EFB6`，`/pay` 返回 `PAYING/prepayPayNo=PAY20260619161949f99cd7d671c0/hasPaymentParams=true`，付款前详情无券码，探针单已取消。当时缺口是真实微信支付成功后本地出票、真实支付退款、商米/后台核销后订单详情 3 秒静默刷新和后台实例/流水一致性。
- 2026-06-19 16:44 再次主动同步后端，`origin/uat` 已到 `fde97b8 docs(frontend-api): 提交6月18日大复核文档`；diff 仅新增后台/小程序复核文档，不触达 Controller/DTO/Service/Mapper 或发布记录，不需要小程序接口适配。显式创建探针 `ticket-appid-pay-mqko7-07..09` 创建订单 `TKT20260619164006248E4940`，`/pay` 返回 `PAYING/prepayPayNo=PAY202606191640066a063a0b4384/hasPaymentParams=true`，支付参数摘要显示 `paymentParams.appId=wx72b9e08ce45d3e79`、`package` 前缀为真实 `prepay_id=wx191640`、`signType=RSA`；付款前详情无券码，探针单已取消。当时缺口是真实微信支付成功后本地出票、真实支付退款、商米/后台核销后订单详情 3 秒静默刷新和后台实例/流水一致性。
- 2026-06-19 17:08 同一 `origin/uat@fde97b8` 运行态复验出现 `/pay` 回归：默认探针 `ticket-closure-mqkphy87-01..04` 商品、日历、报价、确认单均 200；显式创建探针 `ticket-cleanup-mqkpi` 和 `ticket-pay-regress-mqkpj` 均成功创建 `PENDING_PAYMENT` 订单，但 `/pay` traceId=`ticket-cleanup-mqkpi-06`、`ticket-pay-regress-mqkpj-06` 均返回 `INTERNAL_ERROR /api/pay/prepay failed`，无支付参数；探针单 `TKT2026061917083350C6EB34`、`TKT20260619170848E3023085` 已由脚本自动取消为 `CANCELED`。当前小程序不需要改支付入参，阻塞先回到后端 pay-service 预下单稳定性。
- 2026-06-19 17:35 主动同步后端到 `origin/uat@b4fb3f7`，其中 `94d87f6` 已对齐小程序订单支付 AppID 透传：BFF `/orders/{orderNo}/pay` 接收 `OrderPaymentRequest.appId`，pay-service `PrepayRequest.appId` 传入支付记录，微信 JSAPI 下单优先使用请求 AppID；`b4fb3f7` 回填发布记录。默认探针 `ticket-closure-mqkqhaj0-01..04` 商品、日历、报价、确认单仍 200；显式创建探针 `ticket-appid-fix-01..08` 创建订单 `TKT202606191735589114167C` 成功为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-appid-fix-06` 仍返回 `INTERNAL_ERROR /api/pay/prepay failed`，无 `paymentParams/payParams`，探针单已由 `ticket-appid-fix-08` 取消。结论：前后端 AppID 字段已经对齐，最新阻塞仍是后端 pay-service/微信预下单运行态失败。
- 2026-06-19 17:57 后端 `origin/uat@a561839` 后复跑票务探针：该后端提交只改 CRM 入口动作字段和券/K 币错误透传，不触达票务；默认探针 `ticket-closure-mqkr95l0-03..06` 继续证明商品、日历、报价、确认单 200；显式创建探针 `ticket-after-a561839-01..08` 创建订单 `TKT20260619175746535F578F` 成功为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-after-a561839-06` 仍返回 `/api/pay/prepay failed`，无 `paymentParams/payParams`，探针单已取消。当前阻塞仍是 pay-service/微信预下单。
- 2026-06-19 18:09 插队复核 AppID 后再次跑票务探针：源码中登录换 token、旧预支付和统一订单 `/pay` 的当前小程序 AppID 均由 `resolveCurrentMiniProgramAppId()` 统一组包；探针 `ticket-appid-runtime-01..08` 商品、日历、报价、确认单和订单创建仍 200，订单 `TKT20260619180950513F4AC8` 为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-appid-runtime-06` 仍返回 `/api/pay/prepay failed`，无支付参数，探针单已取消。当前阻塞仍是后端 pay-service/微信预下单，不进入真实支付验收。
- 2026-06-19 18:21 主动同步后端到 `origin/uat@3b5c884`，该提交只修 admin-config 券模板目标对象和互斥组同步，不触达票务；探针 `ticket-after-3b5c884-01..08` 商品、日历、报价、确认单和订单创建仍 200，订单 `TKT20260619182138ADD42AFD` 为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-after-3b5c884-06` 仍返回 `/api/pay/prepay failed`，无支付参数，探针单已取消。当前阻塞仍是后端 pay-service/微信预下单，不进入真实支付验收。
- 2026-06-19 18:57 主动同步后端到 `origin/uat@a0341e2`，该提交只补 promotion 历史会员券同源资产回填，不触达票务；探针 `ticket-after-a0341e2-01..08` 商品、日历、报价、确认单和订单创建仍 200，订单 `TKT2026061918575095AEB3DD` 为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-after-a0341e2-06` 仍返回 `/api/pay/prepay failed`，无支付参数，探针单已取消。当前阻塞仍是后端 pay-service/微信预下单，不进入真实支付验收。
- 2026-06-19 19:42 主动同步后端到 `origin/uat@b2a8a37`，该提交只改 admin-config 会员补录手机号区号查重，票务源码无新增；运行态探针 `ticket-after-b2a8a37-03..10` 显示 `GET /api/bff/tickets/products`、单商品 `/calendar` 和 `POST /api/bff/tickets/quote` 均 500，`orders/confirm` 与创建订单仍 200，订单 `TKT20260619194203B06C3B9B` 为 `PENDING_PAYMENT`，`/pay` traceId=`ticket-after-b2a8a37-08` 仍返回 `/api/pay/prepay failed`，无支付参数，探针单已取消。当时阻塞先回到后端恢复票务 BFF 读接口和 pay-service 预下单；该读接口退化已被 20:25 复跑覆盖。
- 2026-06-19 20:25 后端仍停在 `origin/uat@839f4a5`，无新增票务源码；运行态复核 `ticket-after-839f4a5-recheck-20260619202509-03..10` 显示 `products/calendar/quote/confirm/create` 均 200，订单 `TKT202606192025104781708D` 创建为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-after-839f4a5-recheck-20260619202509-08` 仍返回 `/api/pay/prepay failed` 且无支付参数，探针单已取消。当前票务读接口已恢复，小程序没有可适配新字段，后端阻塞收敛为 pay-service/微信预下单稳定性。
- 2026-06-19 21:22 后端复拉到 `origin/uat@f56a059`，本批只改会员查询性能、折扣券同源字段和多业态券同源场景，不触达票务源码；运行态复核 `ticket-after-f56a059-20260619212212-01..08` 显示 `products/calendar/quote/confirm/create` 均 200，订单 `TKT20260619212212B6FB3624` 创建为 `PENDING_PAYMENT`，但 `/pay` traceId=`ticket-after-f56a059-20260619212212-06` 仍返回 `/api/pay/prepay failed` 且无支付参数，探针单已取消。当前票务读接口稳定，小程序没有可适配票务字段，后端阻塞收敛为 pay-service/微信预下单稳定性。

### 优惠券闭环最新复核

- 2026-06-19 本轮续跑券闭环：后端 `origin/uat` 已复拉到 `f56a059`，其中 `a561839` 已补 CRM 入口动作字段源码和券/K 币错误透传，`bcee91a/fb5082c` 收紧管理端会员发券小程序身份校验，`a0341e2` 在 promotion 读取会员券、可用券和退款返还前自动回填历史 CRM 会员券同源资产，`3cdc8b4/7eb18be` 关闭发券任务 READY 假闭环，`212a323/839f4a5` 把 K 币兑换商品限定为 `item_type='EXCHANGE'` 并回填 UAT 文档，`54fcd24/6e3623a` 把后台折扣券模板同源字段同步到 promotion 的 `discountPercent/maxDiscountCent`，`248198f/f56a059` 把多业态/多场景券同源放宽为 promotion `sceneType=ALL`。严格执行 `yarn probe:coupon-closure`，`member/coupons`、`promotion/coupons/available`、`member/coupon-packages`、`member/kcoin/balance` 均返回 200，最新 traceId=`coupon-after-212a323-20260619-01..04`，但当前会员 `couponCount=0`、可用券 0、券包 0、K 币 0，目标券 `CP1781194544162039A54BA` 不在我的券和下单可用券；登录态 CRM 展示入口在 `1d9b386/b34aa63` 复验仍为动作字段 `null`；小程序已把领券中心 K 币 tab 改为读取 `GET /api/bff/crm/entries/exchanges`，并已承接 `discountPercent/maxDiscountCent` 展示折扣券面；当前剩余不是前端页面适配问题，而是后端需补 UAT CRM 动作字段配置或数据迁移、成功 claim/exchange 样本、可复验目标券号和 K 币余额样本。
- 2026-06-17 18:08 按 `$hk-api` 拉取后端仍为 `320a014` 且无新增票务 release；但 UAT 运行态已修复到确认单层。根探针 `GET /api/bff/tickets/products` 命中水上项目快速通，日历 `availableStock=1000`，报价 6800，统一订单确认单 200 且应付 6120，traceId=`ticket-closure-mqhwoxc1-01..04`。
- 小程序当前统一订单确认/创建继续按 `2026-06-13-bff-order-payment-signature-compat.md` 走 Bearer-only，不再为 confirm/create/pay 强制 HMAC；根探针也已同步该口径，并兼容小程序本地存储只有 `csession` 没有显式 `accessToken` 的情况。
- 2026-06-18 11:52 创建探针失败口径已更新：目标 SKU 已配置 `zhiyoubaoGoodsCode=PST20141126001196`，小程序和根探针已同步 `context.certificateNo`。`TICKET_PROBE_CREATE=1 TICKET_PROBE_STRICT=1 node scripts/probe-ticket-closure.mjs` traceId=`ticket-closure-mqiyobbo-05` 显示智游宝 `SEND_CODE_REQ` 返回 `code=0/description=成功/assistCheckNo=2519370003`，但 BFF 返回 `INTERNAL_ERROR`，本地未落订单；DB 只读复核确认 `park_order_fulfillment.fulfillment_status` 仍为旧约束，不允许 order-service 成功出票后写入 `WAIT_USE`。小程序不能展示出票成功，需后端修复 UAT schema/Flyway 或状态映射后再复验创建单进入 `WAIT_USE` 并返回可用 `ticketVouchers[]`。
- 2026-06-17 23:27 后端 `origin/uat@a81410c` 增补《小程序票务订单核销状态轮询比对》：小程序订单详情 3 秒后台静默轮询无需新增后端路径，继续读取 `GET /api/bff/orders/{orderNo}`；轮询响应只做状态指纹探测，不直接渲染，发现变化后再走正常订单详情刷新入口更新页面；后端会通过智游宝订单状态回调更新 `park_order_fulfillment` 的 `ticketStatus/usedNum/totalNum` 并推进订单状态。当前履约状态约束、历史核销回读和 `/pay` 预下单已验证通过，但真实支付成功后的出票与核销后刷新仍需微信开发工具端到端验证。
- 最新直连诊断 traceId=`ticket-closure-mqhwqdmz-05..17` 显示有效票项和主要字段变体已 200，缺日期/零数量/无效票项/无效商城/无效酒店均稳定 400；仅不传 `skuId` 仍 500。当前小程序会传 `skuId`，所以小程序侧无需为当前 SKU 再改请求字段；本轮仅补强 `ticketVouchers[].rawFields` 票码/二维码兜底读取，严格闭环仍要求后端正式字段可用。

## BFF 主接口落地表

| 能力 | 接口 | 鉴权模式 | 小程序落点 | 适配状态 |
| --- | --- | --- | --- | --- |
| 小程序登录 | `POST /api/bff/auth/mini-program/login` | 免认证、免签名 | `src/core/request/index.ts`、`src/core/services/auth.ts` | 已切到 Feishu 路径，存储 `accessToken/refreshToken/signSecret`；授权和 JSON 写请求已显式传 `content-type: application/json`；本接口只代表 BFF token 可用，成功后立即用当前 token 调 `member/status` 刷新 MobX `memberInfo` |
| 刷新令牌 | `POST /api/bff/auth/refresh` | 免认证、免签名 | `src/core/request/index.ts`、`src/core/services/auth.ts`、`src/core/services/bff-api.ts` | service 已接入；统一 request 层已识别 `AUTH_TOKEN_EXPIRED/AUTH_TOKEN_INVALID/AUTH_TOKEN_SESSION_EXPIRED`、`401/10008` 等凭证失效信号，自动用 `refreshToken` 刷新 accessToken/refreshToken/signSecret 后重放原请求一次，并在 token 写入后静默调 `member/status` 校准全局会员态 |
| 会员状态 | `GET /api/bff/auth/member/status` | 登录态 | `src/core/services/auth.ts`、`src/core/services/bff-api.ts`、`src/core/store/member-store.ts` | 已接入；`memberInfo.phone` 是会员是否登录事实源，启动默认拉一次并缓存到 `rootStore.memberInfo`；受保护入口只读缓存态拦截，后续页面和组件统一通过 `rootStore.isLoggedIn`、`rootStore.memberInfo` 渲染头像昵称手机号等级 |
| 手机号授权 | `POST /api/bff/auth/mini-program/phone/authorize` | 登录态 + HMAC 签名 | `src/core/components/LoginPopup`、`src/core/wechat/auth.ts`、`src/core/services/auth.ts`、`src/core/services/bff-api.ts` | 已接入微信 `getPhoneNumber` 新版 `code`；不再使用微信资料登录、本地会员登录或旧 `encryptedData/iv` 作为登录事实；授权成功后用当前 session 刷新 `member/status` |
| 登出 | `POST /api/bff/auth/logout` | 登录态 + HMAC 签名 | `src/core/services/auth.ts`、`src/core/services/bff-api.ts` | 已在退出登录时优先调用后端，失败不阻塞本地退出 |
| 图片上传 | `POST /api/bff/files/images` | 登录态，multipart | `src/core/services/bff-api.ts`、`src/pkg-member/services/profile.ts` | 已接入头像上传；只使用后端返回的 `imageUrl`，不再把本地临时路径作为头像结果 |
| 模块状态 | `GET /api/bff/modules` | 登录态 | `src/core/services/bff-api.ts` | service 已接入，暂无页面直接依赖 |
| 后台默认配置 | `GET /api/bff/admin-config/defaults` | 登录态 | `src/core/services/bff-api.ts` | service 已接入，暂无页面直接依赖 |
| 创建预支付单 | `POST /api/bff/pay/prepay` | 登录态 + HMAC 签名 | `src/core/services/bff-api.ts` | service 已接入；已切统一订单支付的链路必须只使用后端支付参数，缺参阻断 |
| 查询支付状态 | `GET /api/bff/pay/payments/{payNo}` | 登录态 | `src/core/services/bff-api.ts` | service 已接入，待订单详情/支付回查链路使用 |
| 促销试算 | `POST /api/bff/promotion/quote` | 登录态 + HMAC 签名 | `src/core/services/bff-api.ts` | service 已接入，待票务/商城/餐饮结算链路使用 |
| 锁定优惠 | `POST /api/bff/promotion/lock` | 登录态 + HMAC 签名 | `src/core/services/bff-api.ts` | service 已接入，待下单链路使用；Feishu BFF 转换稿漏写 body，按 uat 源码和内部 promotion 文档保留请求体 |
| 核销优惠 | `POST /api/bff/promotion/confirm` | 登录态 + HMAC 签名 | `src/core/services/bff-api.ts` | service 已接入，待支付成功链路使用；Feishu BFF 转换稿漏写 body，按 uat 源码和内部 promotion 文档保留请求体 |
| 释放优惠 | `POST /api/bff/promotion/release` | 登录态 + HMAC 签名 | `src/core/services/bff-api.ts` | service 已接入，待订单取消/超时链路使用；Feishu BFF 转换稿漏写 body，按 uat 源码和内部 promotion 文档保留请求体 |
| 我的优惠券 | `GET /api/bff/member/coupons` | 登录态 GET，不需要签名 | 已新增/收口 `src/core/services/bff-coupon-api.ts`、`src/pkg-member/services/coupons.ts` | 后端已存在接口；页面只读真实会员券资产，不失败回本地券数据；2026-06-18 00:23 复核已补 `page/size/list/total/hasMore/statusCounts/source/sourceName/packageNo/refundReturnStatus`，小程序按 `page=1&size=100` 和 `list` 优先适配，仍需目标 `couponNo` 严格探针 |
| 我的券包 | `GET /api/bff/member/coupon-packages` | 登录态 GET，不需要签名 | 已新增/收口 `src/core/services/bff-coupon-api.ts`、`src/pkg-member/services/coupon-center.ts` | 后端已存在接口；领券中心免费券包已接入，券包内券必须能在我的券和下单可用券按同一 `couponNo` 查询 |
| 领取优惠券 | `POST /api/bff/promotion/coupons/claim` | 登录态 + HMAC 签名 | 已新增/收口 `src/core/services/bff-coupon-api.ts`、`src/pkg-member/pages/coupon-center/index.tsx` | 后端已存在接口；当前真实入参为 `templateNo`，前端不传会员身份字段；领取成功必须进入我的券和下单可用券 |
| 可用优惠券 | `GET /api/bff/promotion/coupons/available` | 登录态 GET，不需要签名 | 已收口到 `src/core/services/bff-coupon-api.ts` 并接入门票/酒店确认单 | 后端最新 `c379081` 已核验，其中 `27c68ed` 已补 `itemIds/skuIds/visitDate/checkInDate/checkOutDate` 透传和 promotion 侧商品/SKU 目标过滤，`ad09fb4` 已补订单 confirm / promotion quote 候选目标校验；小程序已承接 `available/unavailableReason/discountAmount/priority/mutexGroup`，门票/酒店选择券后通过统一订单 `selectedCouponNos` 重新确认，金额以后端 confirm 为准；仍需目标 `couponNo` 严格探针 |
| K 币余额 | `GET /api/bff/member/kcoin/balance` | 登录态 GET，不需要签名 | 已新增/收口 `src/core/services/bff-coupon-api.ts`、`src/pkg-member/pages/exchange-detail/index.tsx` | 后端已存在接口；兑换详情页展示真实余额 |
| K 币流水 | `GET /api/bff/member/kcoin/ledgers` | 登录态 GET，不需要签名 | 已新增/收口 `src/core/services/bff-coupon-api.ts` | 后端已存在接口；后续 K 币明细页使用 |
| K 币兑换 | `POST /api/bff/member/kcoin/exchanges` | 登录态 + HMAC 签名 | 已新增/收口 `src/core/services/bff-coupon-api.ts`、`src/pkg-member/pages/exchange-detail/index.tsx` | 后端已存在接口；源码已在写 `crm_member_coupon_instance` 后同步同一 `couponNo` 到 `promotion_member_coupon`，但当前会员 K 币余额和目标兑换样本为空，页面成功提示仍克制，未跑写入严格探针前不宣称兑换券可下单使用 |
| 商城展示 | `GET /api/bff/mall/home/products/categories/recommendations/gifts/available` | 登录态 GET，不需要签名 | 已新增/收口 `src/core/services/bff-mall-api.ts`、`src/pkg-mall/services/bff-adapter.ts` 和商城首页、分类、列表、详情、推荐、赠品页面 service | 后端 `c379081` 已修复空态 500；最新 UAT `mall-after-e3138d9-01..05` 全部 200 空数组/空分页。页面继续真实接口空态，不回退本地商城数据；商品详情和加购仍需后台真实商品样本验收 |
| 商城购物车 | `GET /api/bff/mall/cart`、`GET /api/bff/mall/cart/count`、`POST/PATCH/DELETE /api/bff/mall/cart/items` | GET 登录态；写接口登录态 + HMAC 签名 | 已新增/收口 `src/core/services/bff-mall-api.ts`、`src/pkg-mall/services/cart.ts`、`src/pkg-mall/pages/cart/index.tsx` | 后端 `c379081` 已修复空购物车 500；最新 UAT `mall-after-e3138d9-06..07` 均 200，`cart` 返回 `groups/items/summary.totalQuantity/totalQuantity/recommendProducts` 空结构。购物车页不再写运行时本地缓存，新增、改数量、勾选、删除和结算草稿仍等真实商品样本验收 |
| 节假日同步 | `POST /api/bff/xxl-job/holiday/sync` | 登录态 + HMAC 签名 | `src/core/services/bff-api.ts` | service 已接入，仅作受控调试入口，不建议普通页面暴露 |
| 酒店列表 | `GET /api/bff/hotels` | 登录态 GET，不需要签名 | `src/pkg-hotel/services/bff-api.ts` | 已接入酒店首页，后端酒店资料是小程序酒店 Tab 和酒店基础信息事实源；无酒店返回空态，不回退本地酒店数据 |
| 酒店详情 | `GET /api/bff/hotels/{hotelId}` | 登录态 GET，不需要签名 | `src/pkg-hotel/services/bff-api.ts` | 已接入房型详情页，按 `hotelId + roomTypeId` 定位，找不到直接异常态 |
| 酒店房型 | `GET /api/bff/hotels/{hotelId}/rooms` | 登录态 GET，不需要签名 | `src/pkg-hotel/services/bff-api.ts` | 已接入首页房型列表和详情页，`roomTypeId/ratePlans/filterKeys/galleryImages` 归一为页面模型 |
| 酒店房态价量 | `GET /api/bff/hotels/{hotelId}/inventory` | 登录态 GET，不需要签名 | `src/pkg-hotel/services/bff-api.ts` | 已接入酒店首页和房型详情；库存查询按入住日至离店前一日取夜间库存，停卖、关到、关离或非在售按不可售处理 |
| 门票商品列表 | `GET /api/bff/tickets/products` | 登录态 GET，不需要签名 | `src/pkg-ticket/services/ticket-api.ts`、`src/pkg-ticket/services/ticket-booking.ts` | 已接入门票预订页；`productType/categorySection=fastPass` 展示快速通，`publishStatus=draft/pendingReview` 只展示待上线，接口失败进入异常态，不回退旧购票菜单 |
| 门票库存日历 | `GET /api/bff/tickets/products/{productCode}/calendar` | 登录态 GET，不需要签名 | `src/pkg-ticket/services/ticket-api.ts`、`src/pkg-ticket/services/ticket-booking.ts` | 已按所选游玩日期归一价格、库存和可售状态；草稿/待审核商品空分页展示待上线或暂无可售日期，已发布且 `saleStatus=onSale` 才允许加购 |
| 门票报价 | `POST /api/bff/tickets/quote` | 登录态 + HMAC 签名 | `src/pkg-ticket/services/ticket-api.ts`、`src/pkg-ticket/services/checkout.ts` | 已接入门票确认单；金额、库存和限制原因以后端报价为准，接口失败阻断确认单 |
| 统一订单确认 | `POST /api/bff/orders/confirm` | 登录态 Bearer，不需要 HMAC 签名 | `src/core/services/bff-order-api.ts`、`src/pkg-hotel/services/checkout.ts`、`src/pkg-ticket/pages/checkout/index.tsx` | 已按 `2026-06-13-bff-order-payment-signature-compat.md` 移除前端强制签名；酒店确认单已接入；票务确认单已按 `OrderUnifiedRequest` 提交真实商品、SKU、日期和出游人字段。2026-06-17 18:08 最新运行态确认单 200，应付 6120；直连诊断有效票项和主要字段变体均 200，缺日期、零数量、无效票项/商城/酒店均稳定 400，仅不传 `skuId` 仍 500；当前小程序会传 `skuId`，确认单层不再是当前阻塞 |
| 统一订单创建 | `POST /api/bff/orders` | 登录态 Bearer，不需要 HMAC 签名 | 同上 | 已移除前端强制签名；酒店提交订单已接入；票务创建订单已同步 `context.certificateNo` 兼容当前后端证件读取；后端 `origin/uat@d92be1e` 后门票创建只返回待支付订单，不再直接出票 |
| 统一订单支付 | `POST /api/bff/orders/{orderNo}/pay` | 登录态 Bearer，不需要 HMAC 签名 | `src/core/services/bff-order-api.ts`、`src/pkg-hotel/pages/checkout/index.tsx`、`src/pkg-order/pages/detail/index.tsx`、`src/pkg-ticket/services/order-draft.ts` | 已移除前端强制签名；酒店提交后和订单详情继续支付均使用真实支付参数；门票创建后必须调用 `/pay`，只使用后端 `prepay.paymentParams/payParams` 拉起微信支付，不再识别 `paymentSkipped`；缺支付参数直接阻断，不走本地模拟支付。前端传当前小程序 AppID 时只允许 `resolveCurrentMiniProgramAppId()` 内部组包，外部入参不能传自定义 AppID。2026-06-19 16:44 UAT 门票 `/pay` 曾返回微信预支付参数且 `paymentParams.appId=wx72b9e08ce45d3e79`；21:22 最新探针 `products/calendar/quote/confirm/create` 均 200，但订单 `TKT20260619212212B6FB3624` 的 `/pay` traceId=`ticket-after-f56a059-20260619212212-06` 仍返回 `/api/pay/prepay failed`，当前等待后端恢复 pay-service 预下单稳定 |
| 商米快速通票码核销 | `POST /api/bff/verifier/ticket-verifications/verify`、`GET /api/bff/verifier/ticket-verifications` | 公开，不需要 Authorization 或签名 | 小程序 App 不直接调用；商米 APK/核销设备使用，订单详情通过 `GET /api/bff/orders/{orderNo}` 被动感知状态变化 | 后端 `18b68f0` 已回填 UAT 发布结果；`GET /api/bff/verifier/ticket-verifications?page=1&pageSize=1&deviceNo=uat-smoke-device` 实测 `200 OK` 空列表。请求/响应沿用 `couponCode/deviceNo` 和 `CouponVerificationResponse` 形状，但业务含义是快速通票码。小程序只需保证订单详情 3 秒静默轮询和票码状态展示正确，不能在小程序本地伪造核销成功 |

## 购票/CMS 后续变更落地表

| 页面/能力 | 接口 | 鉴权模式 | 字段归一 | 适配状态 | 验证状态 |
| --- | --- | --- | --- | --- | --- |
| 门票预订商品 | `GET /api/bff/tickets/products` | 登录态 GET，不需要签名 | `productCode/skuRules/categorySection/publishStatus/channels` 归一为页面票种、快速通和待上线状态 | 已接入 `src/pkg-ticket/services/ticket-api.ts` 和 `ticket-booking.ts`；旧 `purchase/menus` 不再作为门票商品事实源；前端兜底过滤非小程序渠道商品，避免后台未勾选 `miniProgram` 的商品出现在小程序 | 2026-06-16 微信开发工具确认进入购票页后返回 200；页面可见已发布商品、快速通和草稿/待审核待上线状态 |
| 门票日历和报价 | `GET /api/bff/tickets/products/{productCode}/calendar`、`POST /api/bff/tickets/quote` | 日历为登录态 GET；报价为登录态 + HMAC 签名 | `date/skuId/price/availableStock/saleStatus/restrictionReason` 映射库存、价格、禁用原因；`requiredFields/mobileRequired/certificateRequired/travelerRoles` 控制确认单出游人字段；报价金额以后端返回为准 | 预订页按日期刷新真实库存；同一页面初始化内已按 `productCode + dateRange` 合并日历请求，草稿/待审核商品不再请求可售日历；短 TTL 缓存只淘汰已完成结果，进行中的同 key 日历请求会一直复用，避免慢接口或初始化补偿触发同一 calendar 连打；确认单已接报价；0 元票和免实名快速通按已选数量提交，不再用金额或本地强制实名阻断 | 2026-06-16 微信开发工具确认日历只对 5 个已发布商品各请求 1 次，未再出现 `TICKET_FAST_PASS_SIGHTSEEING_TRAIN/calendar` 连打 10 次；水上项目快速通 2026-06-16 `totalStock=20/availableStock=20` 且页面加号视觉可用。2026-06-17 先复核今天至未来 30 天 5 个已发布商品均无正库存，随后后台单日库存 upsert 水上项目快速通 2026-06-17 为 `totalStock=20/availableStock=20`，小程序 BFF 日历回读同日库存 20，`POST /api/bff/tickets/quote` 返回 `availableStock=20/payableAmountCent=6800`；加购可见性已不再阻塞，后续阻塞转为统一订单确认 500 |
| 购票页资源位 | `GET /api/bff/purchase/resources?sceneType=TICKET&pageCode=PURCHASE_HOME` | 登录态 GET，不需要签名 | `mobileImageUrl/imageUrl -> heroImages` | 已接入 `src/pkg-ticket/services/ticket-booking.ts`，由默认 request 先拿 token；资源位失败或无图按空态处理，不再补本地图片 | 2026-06-16 微信开发工具确认返回 200；当前资源位图片存在 UAT 404，需要后台或素材侧修复 |
| CMS 单资源位 | `GET /api/bff/cms/resources/{slotCode}` | 登录态 GET，默认携带 `Authorization` | `slotCode` 作为资源位稳定 key | `src/pkg-ticket/services/purchase-api.ts` 已接 service；暂无页面直接依赖；后端 `c06ca8f` 后不再按公开接口调用 | `curl` 无 token 返回 `401 AUTH_TOKEN_MISSING`；带 token 待验证 |
| 首页广告聚合 | `GET /api/bff/content/mini-program/ads?pagecode=index` | 登录态 GET，不需要签名 | `slots[].slotCode` 作为资源位 key；广告图片按 `backgroundImage/materialImage/iconImage/imageUrl/mobileImageUrl` 归一；跳转按 `jumpType + jumpPath/jumpUrl/jumpCustomValue/jumpAppId` 归一 | 已新增 `src/core/services/mini-program-ad.ts`，首页 `src/pages/home/index.tsx` 读取聚合接口；默认 request 会先完成小程序授权并携带 `Authorization`；`index_top_banner` 优先覆盖顶部轮播，无数据才兼容旧 `index_banner`；`index_nav_grid/index_schedule/index_hot_project/index_activity/index_recommend/index_member_benefit/index_play_life` 覆盖对应首页楼层；接口失败或核心资源位无数据时进入异常态，不再回退本地旧内容 | 2026-06-08 后台真实聚合已回读：首页顶部 Banner 5、八大导航 8、节目单 1、热门项目 2、精选活动 1、精彩推荐 2、会员福利 1、吃喝玩乐 9；无小程序登录态直接请求 BFF 返回空 slots，待微信开发工具网络面板确认携带 `Authorization` 后目视首页渲染 |
| 单广告详情 | `GET /api/bff/content/mini-program/ads/{id}` | 登录态 GET，不需要签名 | `id` 使用后端数字 ID；详情返回 `page/slot` 上下文；详情正文只允许把后端 `richTextHtml/richText` 原文直接传给小程序 `RichText`，不在前端重写 HTML 结构，也不拼装标题、图片或描述作为正文 | 已在 `src/core/services/mini-program-ad.ts` 补齐详情函数；首页广告点击进入活动/项目/节目单详情时必须把后端广告 `id` 写入路由，`activity-detail`、`park-detail` 和 `schedule` 用该 `id` 回查详情；接口失败、无数据或缺少富文本正文进入异常态，不再回退本地旧详情 | `yarn typecheck` 通过；待登录态回归验证 |
| 资源位广告列表 | `GET /api/bff/content/mini-program/slots/{slotCode}/ads` | 登录态 GET，不需要签名 | `slotCode` 使用首页“查看全部”路由参数；广告字段复用 `MiniProgramAdView`，前端按 `sortOrder` 升序展示 | 已新增 `fetchMiniProgramSlotAds(slotCode)`；首页热玩榜单、精选活动、精彩推荐、玩转乐园的“查看全部”会携带 `slotCode/title`；`park-list` 和 `activity-list` 按资源位直查真实接口并删除旧本地 mock 数据 | 待微信开发工具用真实小程序登录态验证 `/slots/index_hot_project/ads`、`/slots/index_activity/ads` 等请求携带 `Authorization` |

## CRM/P1 UAT 变更落地表

| 页面/能力 | 接口 | 鉴权模式 | 字段归一 | 适配状态 | 验证状态 |
| --- | --- | --- | --- | --- | --- |
| 会员中心首页 | `GET /api/bff/crm/center` | 登录态 | `profile.growthValue/couponCount/nextLevel*` 更新首页数值，`benefits` 映射权益区；不读取内部会员标识 | 已接入 `src/core/services/bff-crm-api.ts`、`src/pkg-member/services/index.ts`；接口失败进入页面异常态，不再回本地会员假数据 | `yarn typecheck` 通过；待微信开发工具真实登录态验证 |
| 会员资料查询/保存 | `GET/POST /api/bff/crm/profile` | 查询登录态；保存登录态 + HMAC 签名 | `nickName -> nickname`，`phone -> mobile`，`idCardNo`、`gender UNKNOWN/MALE/FEMALE -> 0/1/2`；BFF 不返回 `memberNo/openId/userId`，前端不再传或读这些字段 | 已接入 `src/pkg-member/services/profile.ts`；资料查询/保存、头像上传和老会员绑定均成功后同步 `rootStore.memberInfo`，保存后用当前 session 刷新 `member/status`；失败不再兜底本地会员资料 | `yarn typecheck` 通过；待真实签名写接口验证 |
| 我的地址 | `GET/POST /api/bff/crm/addresses`、`POST /delete`、`POST /default` | 查询登录态；写接口登录态 + HMAC 签名 | `addressNo -> id`，`contactName -> name`，省市区名称拼 `region`，`detailAddress -> detail` | 已接入 `src/pkg-order/services/address.ts`；列表、保存、删除和默认地址均以真实接口结果为准，接口失败直接暴露错误，不再先写本地再异步同步 | `yarn typecheck` 通过；待真实签名写接口验证 |
| 会员码 | `GET /api/bff/crm/member-code` | 登录态 | 只使用 `qrContent`，BFF 不再暴露 `memberNo` | 已接入 `src/pkg-member/services/member-code.ts`；缺 `qrContent` 或接口失败进入异常态，不再生成本地动态码 | `yarn typecheck` 通过；待真实登录态验证 |
| 老会员绑定 | `POST /api/bff/crm/legacy-bind` | 登录态 + HMAC 签名 | `phone` 入参，结果 `bound` 映射老会员绑定状态；不读取 `memberNo/legacyMemberNo` | 已接入 `src/pkg-member/services/profile.ts`，绑定成功后重新拉取资料并刷新 `member/status` | `yarn typecheck` 通过；待真实签名写接口验证 |
| 领券中心入口 | `GET /api/bff/crm/entries/coupons` | 登录态 | `itemNo/itemName/badgeText/tagText/pointsCost/extraPayload.buttonText` 映射券卡 | BFF service 路径已从 `/crm/p1/coupons` 改为 `/crm/entries/coupons`；页面数据只来自真实接口，接口失败异常态、无券空态，不回退本地券卡 | `yarn typecheck` 通过；待微信开发工具验证 |
| 兑换专区入口 | `GET /api/bff/crm/entries/exchanges`、`GET /api/bff/crm/entries/items/{itemNo}` | 登录态 | `itemNo/itemName/imageUrl/pointsCost/stockAvailable/description` 映射兑换商品 | BFF service 路径已从 `/crm/p1/**` 改为 `/crm/entries/**`；列表和详情只来自真实接口，兑换提交在后端缺少真实提交接口前阻断，不展示本地成功 | `yarn typecheck` 通过；待微信开发工具验证 |

## 优惠券 BFF 专项落地表

字段级后端必补清单：`admin-frontend/docs/codex/admin-api-requirements/mp-bff-requirements/coupon-bff-required-interfaces-2026-06-16.md`。

页面路由到接口映射：`admin-frontend/docs/codex/admin-api-requirements/mp-bff-requirements/route-api-map.md`。

| 页面/能力 | 接口 | 鉴权模式 | 字段归一 | 适配状态 | 验证状态 |
| --- | --- | --- | --- | --- | --- |
| 我的优惠券 | `GET /api/bff/member/coupons` | 登录态 GET | `couponNo/templateNo/couponName/thresholdAmountCent/discountAmountCent/status/validStartAt/validEndAt/sceneType/source/sourceName/packageNo/refundReturnStatus` 映射券卡和 tab | 已替换 `src/pkg-member/services/coupons.ts` 本地运行时券数据；接口失败异常态、无券空态；已按新版分页响应读取 `list` | 本轮 `yarn typecheck`、`yarn check:package-boundary`、`git diff --check` 通过 |
| 我的券包 | `GET /api/bff/member/coupon-packages` | 登录态 GET | `packageNo/packageName/packageStatus/couponNos/coupons` 映射领券中心免费券包 | 已落 `src/core/services/bff-coupon-api.ts` 和 `src/pkg-member/services/coupon-center.ts`；页面不再用假领取 toast | `yarn typecheck` 已通过；券包实例同源待后端验收 |
| 领券动作 | `POST /api/bff/promotion/coupons/claim` | 登录态 + HMAC 签名 | 当前真实 `templateNo` 提交；响应 `coupon` | 已把领券中心改为真实 claim，成功后刷新列表；前端不传会员身份字段 | `yarn typecheck` 已通过；领取后我的券/可用券同源待后端验收 |
| K 币余额和流水 | `GET /api/bff/member/kcoin/balance`、`GET /api/bff/member/kcoin/ledgers` | 登录态 GET | `pointsBalance/availablePoints/ledgers` 映射兑换详情和后续明细 | 兑换详情已读真实余额，流水 service 已收口待明细页使用 | `yarn typecheck` 已通过 |
| K 币兑换 | `POST /api/bff/member/kcoin/exchanges` | 登录态 + HMAC 签名 | `itemNo/quantity/idempotencyKey` 提交；响应按 `exchangeNo/pointsCost/beforeBalance/afterBalance/couponNos` 读取 | 已接入真实提交；后端源码已同步 promotion 资产，成功提示仍克制，等待目标 `itemNo` 和余额样本做写入探针 | 本轮 `yarn typecheck`、`yarn check:package-boundary`、`git diff --check` 通过 |
| 门票/酒店/商城可用券 | `GET /api/bff/promotion/coupons/available` | 登录态 GET | `couponNo/couponName/status/reason/available/unavailableReason/discountAmount/discountAmountCent/thresholdAmountCent/itemIds/skuIds/visitDate/checkInDate/checkOutDate` 映射确认单券选择 | 已接入门票、酒店和商城确认单；门票传票项、SKU 和游玩日期，酒店传酒店 ID、房型 ID、价规和入住离店日期，商城传商品 ID、SKU 和订单金额；选择后通过统一订单 `selectedCouponNos` 重新确认，创建订单时带同一券号，金额以后端订单确认为准 | 本轮 `yarn typecheck`、`yarn check:page-convention`、`yarn check:package-boundary`、`git diff --check` 通过；后端已补订单试算/确认同目标规则校验，仍需目标券号和微信开发工具验收 |
| 退款返还 | `POST /api/bff/promotion/coupons/refund-return` | 登录态 + HMAC 签名 | `orderNo/refundNo/sceneType/couponNos/refundAmount/refundType/idempotencyKey` | `63e88b5` 已补幂等冲突处理；小程序订单退款已接真实 BFF；券返还状态仍需真实已核销券同源验收 | 待目标券样本验收 |

## 订单 UAT 变更落地表

| 页面/能力 | 接口 | 鉴权模式 | 字段归一 | 适配状态 | 验证状态 |
| --- | --- | --- | --- | --- | --- |
| 订单确认 | `POST /api/bff/orders/confirm` | 登录态 Bearer，不需要 HMAC 签名 | `sceneType/items/contact/paymentChannel/context/selectedCouponNos` 按后端 `OrderUnifiedRequest` 建 service 类型；票务使用 `sceneType=TICKET`、`channel=MINI_PROGRAM`、`selectedCouponNos=[]`、`itemId=productCode`、`skuId=${productCode}_standard`、`context.visitDate`；商城使用 `sceneType=MALL`、商品 ID/SKU、地址上下文和选中券号 | 已用于酒店、门票和商城确认单；商城确认单已接真实 `confirm/create/pay`，选券后重新确认并在创建订单时带同一 `selectedCouponNos`；`src/core/services/bff-order-api.ts` 已按后端兼容契约移除前端强制签名 | `node scripts/probe-ticket-closure.mjs` 已验证票务商品、日历、报价和确认单均 200；商城仍需后台真实商品、目标券号和微信开发工具验收 |
| 门票创建订单 | `POST /api/bff/orders` | 登录态 Bearer，不需要 HMAC 签名 | 使用统一订单 `OrderUnifiedRequest`，票务请求由 `buildTicketUnifiedOrderRequest()` 生成，出游人 JSON 只包含 SKU 要求的实名字段 | 已用于 `pkg-ticket` 下单，前端不再保存本地模拟订单，也不为免实名快速通生成空出游人；`src/core/services/bff-order-api.ts` 已按后端兼容契约移除前端强制签名；`d92be1e` 后创建成功只代表待支付订单成立，不代表已出票 | 2026-06-19 13:25 探针创建订单 `TKT202606191325080E335D16` 成功进入 `PENDING_PAYMENT`，traceId=`ticket-closure-mqkhitex-07`；旧严格探针 `TKT202606181516448D71CD09` 仅作为免支付历史回归证据 |
| 门票支付 | `POST /api/bff/orders/{orderNo}/pay` | 登录态 Bearer，不需要 HMAC 签名 | `paymentChannel=WECHAT`，返回微信支付参数 | 已用于 `pkg-ticket` 支付，缺支付参数时页面阻断，不模拟支付成功；`src/core/services/bff-order-api.ts` 已按后端兼容契约移除前端强制签名；当前小程序 AppID 由 service 内部 `resolveCurrentMiniProgramAppId()` 组包 | 2026-06-19 13:25 和 16:44 UAT 曾返回微信预支付参数，16:44 `paymentParams.appId=wx72b9e08ce45d3e79` 正确；21:22 `origin/uat@f56a059` 探针显示票务 `products/calendar/quote/confirm/create` 均 200，但创建订单后 `/pay` traceId=`ticket-after-f56a059-20260619212212-06` 仍回归 `/api/pay/prepay failed`。先等后端恢复 pay-service 预下单稳定，再用微信开发工具完成真实支付并验收出票和订单详情 `ticketVouchers[]` |
| 创建订单 | `POST /api/bff/orders` | 登录态 Bearer，不需要 HMAC 签名 | 酒店 `sceneType=HOTEL`，`itemId=hotelId`，`skuId=ratePlanId`，`itemType=HOTEL_ROOM`，`attributes` 写 `roomTypeId/checkInDate/checkOutDate/ratePlanId` | 酒店提交订单已接入真实创建；旧 `/submit` 仅保留为历史兼容 service，不再用于酒店 | `yarn typecheck` 通过；待微信开发工具真实支付验证 |
| 发起支付 | `POST /api/bff/orders/{orderNo}/pay` | 登录态 Bearer，不需要 HMAC 签名 | `paymentChannel=WECHAT`，只使用后端 `prepay.paymentParams/payParams` | 酒店提交后支付和订单详情继续支付已接入；缺参数阻断 | `yarn typecheck` 通过；待微信开发工具真实支付验证 |
| 订单详情 | `GET /api/bff/orders/{orderNo}` | 登录态 Bearer，不需要 HMAC 签名 | `orderNo/orderStatus/amount/items/context/ticketVouchers` 归一为订单详情模型 | 订单详情页已切真实 BFF；无数据或接口错误进入异常态；票务待使用凭证页内每 3 秒后台静默轮询该接口，失败不弹窗、不展示 loading，状态指纹不变不触发页面重渲染，探针响应不直接渲染，发现变化后再走正常订单详情刷新入口更新页面；后端确认会透出核销后的 `ticketStatus/usedNum/totalNum` 和订单状态 | 订单 `TKT202606181516448D71CD09` 详情已回读 1 张智游宝凭证，`codeImage/qrImage` 均为可直接渲染的 `data:image/jpeg;base64,...`；同单标准化回调后已回读 `USED/FULFILLED/usedNum=1`，小程序轮询读取路径成立 |
| 订单列表 | `GET /api/bff/orders?sceneType=TICKET/MALL/HOTEL` | 登录态 | 订单中心聚合 `TICKET/MALL/HOTEL` 三类订单并按状态分组 | 订单中心列表已切真实 BFF；单场景接口失败会暴露页面异常，不合并旧本地订单 | `yarn typecheck` 通过 |
| 取消订单 | `POST /api/bff/orders/{orderNo}/cancel` | 登录态 + HMAC 签名 | `orderNo/reason` 提交后端，成功后刷新订单详情 | 订单取消页已切真实 BFF，进入页先查真实订单详情，提交失败直接暴露错误，不再本地模拟取消成功 | 待微信开发工具真实订单验证 |
| 申请退款 | `POST /api/bff/orders/{orderNo}/refunds` | 登录态 + HMAC 签名 | `orderNo/reason` 提交后端，成功后刷新订单详情 | 订单详情退款动作已接真实 BFF；后端未允许退款时按业务错误展示，不跳本地售后成功页 | 历史 UAT 直出票订单 `TKT202606181609268A8727B2` 已退款成功，traceId=`ticket-refund-mqj81o00-01`，详情 traceId=`ticket-refund-mqj81o00-02` 回读 `REFUNDED`；`d92be1e` 后真实微信支付退款需新样本复测 |

## 鉴权跟进

- `src/core/request/index.ts` 已支持 `sign: true`，对 `POST/PUT/PATCH/DELETE` 自动追加 `X-Timestamp`、`X-Nonce`、`X-Body-Sha256`、`X-Signature`。
- `src/core/utils/crypto.ts` 纯小程序运行时实现 SHA-256、HMAC-SHA256 Base64URL no padding 和 nonce，不引入 Node crypto。
- `src/core/store/member-store.ts` 已持久化 `refreshToken` 和 `signSecret`；清空会话时一并清掉签名密钥。
- `src/core/store/member-store.ts` 统一维护 `memberInfo`，头像、昵称、手机号、等级和登录态都从 MobX 派生；页面/组件优先读 `rootStore.memberInfo` 和 `rootStore.isLoggedIn`。
- 普通受保护接口默认携带 `Authorization: Bearer <token>`；截至 2026-06-07，除登录和刷新外所有小程序 BFF 业务接口均需登录态，GET 只校验 token，不需要签名。
- BFF token 只代表后端会话可用，不代表会员已登录；`src/core/services/auth.ts` 的 `getMemberStatus({ isNeedLogin })` 明确区分是否需要前置 login。启动走 `refreshMember()`，即先 login 再 `member/status`；受保护入口只读 `rootStore.isLoggedIn`，未登录直接打开弹窗，不主动重复 login/memberStatus；手机号授权、资料保存和老会员绑定后走 `syncMemberStatus()`，只用当前 token 静默调 `member/status`；request 层 login/refresh token 成功后也只用当前 token 静默调 `member/status`，避免反向循环 login。
- 统一 request 层在业务接口返回 `AUTH_TOKEN_EXPIRED`、`AUTH_TOKEN_INVALID`、`AUTH_TOKEN_SESSION_EXPIRED`、`401` 或旧 `10008` 时，会优先调用 `/api/bff/auth/refresh` 换新令牌并重放原请求一次；refreshToken 缺失或失效时清空后端会话和会员态，后续入口再按后端事实刷新。
- 页面、资源位或业务链路已经进入真实接口联调后，相关运行时 mock/静态业务数据必须删除或隔离到不可达测试夹具；接口失败、字段缺失或配置缺失时必须进入异常态、空态或阻断态，便于前后端定位真实链路问题。
- 登录和刷新接口请求体必须保持 JSON，`src/core/request/index.ts` 已为 JSON 写请求默认带 `content-type: application/json`；后端从 `v0.1.9`/`v0.1.10` 起对错误请求体、错误媒体类型、JWT 签发和 Redis 登录态存储异常返回明确错误码，不再统一落 500。
- Feishu 文档里的 promotion/pay/admin-config/xxl-job 内部服务接口只作为后端/管理后台联调参考，小程序只走 `/api/bff/**`。
- 当前发现 Feishu BFF 转换稿中 `promotion lock/confirm/release` 的请求体遗漏；已按 BFF uat 源码 `@RequestBody Map` 和内部 promotion 接口示例落 service，请后端后续同步修正文档。

## HANDOFF 给 hk-api

| 批次 | 事项 | 已改文件 | 待合并/待确认 | 验证状态 | 演进候选 |
| --- | --- | --- | --- | --- | --- |
| `BE-2026-05-19-feishu-bff-base` | Feishu 主文档 13 个 BFF 对外入口已落小程序 service/request 层，高风险写接口签名已接入 | `src/core/request/index.ts`、`src/core/store/member-store.ts`、`src/core/utils/crypto.ts`、`src/core/services/bff-api.ts`、`src/core/services/auth.ts`、`src/core/config/env.ts` | 根总表同步“小程序已完成 service 层接入，交易链路待页面级接入”；管理后台继续按自身联调文档处理后台配置接口 | `yarn typecheck` 已通过；真实登录/签名写接口需微信开发工具和真实 token 验证 | 后续如果出现第二个端需要签名，抽一个端内签名验收小脚本或示例用例 |
| `BE-2026-05-28-purchase-cms` | 小程序购票列表和购票页资源位已按 uat 后续变更记录端内接入；最新 `936aa38/00486d4` 已恢复登录态 GET | `src/pkg-ticket/services/purchase-api.ts`、`src/pkg-ticket/services/ticket-booking.ts` | 剩余微信开发工具网络面板确认购票列表和购票资源位携带 `Authorization`；购票页资源位失败按异常态或空态处理 | 待新鉴权口径回归 | 接口鉴权必须以最新 BFF 安全配置和 release 为准，不能沿用旧公开接口结论；资源位失败不得用旧图片或旧内容兜底 |
| `BE-2026-05-28-crm-uat` | CRM/P1 小程序接口已落 service 层，会员首页/资料/地址/会员码/领券/兑换已去掉运行时本地兜底 | `src/core/services/bff-crm-api.ts`、`src/pkg-member/services/index.ts`、`src/pkg-member/services/profile.ts`、`src/pkg-member/services/member-code.ts`、`src/pkg-member/services/coupon-center.ts`、`src/pkg-member/services/exchange.ts`、`src/pkg-order/services/address.ts` | 根总表同步“小程序 CRM service 已接入，写接口待真实签名验证”；后台 CRM 配置由管理后台文档跟进；兑换提交需要后端补真实提交接口后再放开 | `yarn typecheck` 已通过；待微信开发工具真实登录态验证 | CRM/P1 字段映射已出现复用模式，后续第二批页面接入时可评估沉淀端内 adapter |
| `BE-2026-05-30-order-checkout-uat` | 订单提交/详情/列表 BFF service 已落地，票务确认/创建/支付已强切统一订单真实接口 | `src/core/services/bff-order-api.ts`、`src/pkg-ticket/services/order-draft.ts`、`src/pkg-ticket/services/checkout.ts`、`src/pkg-ticket/pages/checkout/index.tsx` | 票务订单详情已优先展示后端 `ticketVouchers[]`，旧 `ticketInstances[]` 仅作为历史订单兼容；商城下单待后续工作包接入 | `yarn typecheck` 已通过；待真实订单链路和支付参数验证 | 订单 service 保持通用类型；交易链路不得再回本地订单或模拟支付成功 |
| `BE-2026-06-16-ticket-closure-recovery` | 门票预订恢复闭环：真实 BFF 商品、日历、报价、确认单、订单创建、真实微信支付后出票和订单详情凭证；小程序已把出票成功判断从 `ticketVouchers.length` 改为 `WAIT_USE/FULFILLING + 可用票码或二维码 + 非 FAILED`，本轮补强 `rawFields` 兜底读取，并在订单详情增加 3 秒后台静默轮询覆盖支付后 `PAYING` 异步出票、停留券码页被外部核销不会触发 `onShow` 的场景 | `src/core/services/bff-order-api.ts`、`src/pkg-ticket/services/ticket-api.ts`、`src/pkg-ticket/services/ticket-booking.ts`、`src/pkg-ticket/services/purchase-api.ts`、`src/pkg-ticket/pages/ticket-booking/index.tsx`、`src/pkg-ticket/pages/ticket-booking/index.scss`、`src/pkg-ticket/pages/checkout/index.tsx`、`src/pkg-ticket/services/checkout.ts`、`src/pkg-ticket/services/order-draft.ts`、`src/pkg-order/services/detail.ts`、`src/pkg-order/pages/detail/index.tsx` | 需要 `$hk-api` 合并根总表：小程序票务商品不再走旧 `purchase/menus`；后台已能通过单日库存 upsert 配出小程序可见可售日期，批量 upsert 合成库存仍是后端缺口；21:22 最新探针商品、日历、报价、确认单和创建待支付订单已恢复 200，但 `/pay` 仍回归 `/api/pay/prepay failed`；历史创建即出票阻塞已被 `cf7e4b5` 修复并又被 `d92be1e` 改为真实支付后出票；微信开发工具验收同一对话只复用本对话已打开或接管的当前项目窗口 | 已跑默认探针、直连诊断、运行态诊断、历史严格创建探针、`yarn typecheck` 和 `git diff --check`；2026-06-19 21:22 `ticket-after-f56a059-20260619212212-01..08` 证明读链路和建单恢复，但 `/pay` 仍失败，探针单已取消；2026-06-19 16:55 已把订单详情静默探针状态集补入 `PAYING` | 票务页面已进入真实接口红线，后续不得恢复静态票种、静态日期、模拟订单、本地二维码、直接用轮询探针响应渲染页面或重复请求同一日历资源；接口失败必须暴露阻断态；只有 `/pay` 稳定返回微信支付参数且真实支付后订单详情能读到 `ticketVouchers[]` 才算当前支付口径闭环 |
| `BE-2026-06-18-ticket-voucher-closure` | 后端补齐履约状态约束、智游宝二维码映射、可渲染 data URI、退款终态保护、内部主动查询同步，并在 `d92be1e` 恢复门票真实微信支付；`2e44302` 后 UAT 曾返回门票 `/pay` 微信预支付参数；小程序改为创建订单后调用 `/pay`，支付成功后再用订单详情读取票码/核销码 | `src/core/services/bff-order-api.ts`、`src/pkg-order/services/detail.ts`、`src/pkg-order/pages/detail/index.tsx`、`src/pkg-ticket/pages/checkout/index.tsx`、`src/pkg-ticket/services/order-draft.ts` | 需要 `$hk-api` 合并根总表：历史严格探针已证明旧直出票 `WAIT_USE` 和订单详情凭证；标准化智游宝回调已证明订单详情可回读 `USED/FULFILLED/usedNum=1`；旧直出票退款和退款终态保护已验证；`cf7e4b5` 源码证明内部主动查询会同步 order-service 履约但 BFF 详情未自动调用；`d92be1e` 撤回 `paymentSkipped` 免支付契约，小程序现有代码已同步；当前真实支付口径阻塞在 `/pay` 预下单稳定性，恢复后再验真实支付后出票、真实支付退款和后台票码实例/核销流水同步 | 已跑旧严格探针、退款、迟到回调和核销回调历史证据；`TKT202606191325080E335D16` 和 16:44 `ticket-appid-pay-mqko7-08` 曾返回微信预支付参数且 AppID 正确；2026-06-19 21:22 `ticket-after-f56a059-20260619212212-01..08` 显示 `products/calendar/quote/confirm/create` 均 200，但 `/pay` 仍失败，探针单已自动取消；本轮文档改动后需跑 `git diff --check` | 票务页面继续遵守真实接口红线和 3 秒后台静默轮询规则；后续不得为核销或退款状态本地造终态，必须以后端 `ticketStatus/usedNum/totalNum/orderStatus` 为准；后端需先恢复 pay-service 预下单稳定，再进入微信开发工具真实支付验收出票；若希望无回调时更快刷新，需要将内部主动同步接入定时任务、后台动作或 BFF 安全触发链路 |
| `BE-2026-06-03-login-content-type-token-uat` | 后端登录 Content-Type、请求体解析、JWT 签发和 Redis 登录态存储异常已返回明确错误码；小程序统一 request 已为 JSON 写请求默认补 `content-type: application/json` | `src/core/request/index.ts`、`docs/codex/interface-integration.md`、`docs/codex/current-mini-program.md` | 需要 `$hk-api` 合并根总表最近辅助 commit `37383fb` 和本批次“前端 request header 已补齐”结论 | 待微信开发工具用真实 code 复测登录和刷新，重点观察 `400/415/AUTH_TOKEN_*` 错误码不再表现为通用 500 | 登录失败 toast 如后续需要更业务化，可在 request 层增加错误码文案映射；当前先不为单批次扩写规则 |
| `BE-2026-06-06-cms-auth-tighten-uat` | 后端移除 `/api/bff/cms/**` 公开白名单，单 CMS 资源位恢复默认登录态；后续 `936aa38/00486d4` 进一步恢复所有小程序 BFF 业务接口鉴权 | `src/pkg-ticket/services/purchase-api.ts`、`src/pkg-ticket/services/ticket-booking.ts`、`docs/codex/interface-integration.md`、`docs/codex/current-mini-program.md` | 需要 `$hk-api` 合并根总表最近辅助 commit `936aa38/00486d4`，并覆盖旧“购票资源位公开 GET”结论 | 待微信开发工具目视购票页和网络面板确认携带 `Authorization` | 稳定规则候选：接口鉴权必须以 BFF 最新安全配置和 release 为准；可选资源位接口失败不得拖垮核心列表数据 |
| `BE-2026-06-07-mini-program-ads` | 小程序首页已接入登录态广告聚合接口，按资源位覆盖首页核心内容区域；广告详情页按后端广告 `id` 回查富文本详情；request 层已补齐 accessToken 过期后的 refresh + 原请求重放 | `src/core/request/index.ts`、`src/core/types/mini-program-ad.ts`、`src/core/services/mini-program-ad.ts`、`src/core/utils/ad-click.ts`、`src/pages/home/index.tsx`、`src/pkg-ticket/services/activity.ts`、`src/pkg-ticket/services/park-detail.ts`、`docs/codex/interface-integration.md`、`docs/codex/current-mini-program.md` | 需要 `$hk-api` 合并根总表最近辅助 commit `936aa38/00486d4`，并登记小程序广告接口不再公开；后台已用真实接口上传并投放 5 张首页顶部 banner | `yarn typecheck` 通过；待微信开发工具网络面板确认广告聚合和单广告详情在 token 过期时表现为“原请求 401 -> refresh -> 原请求重放成功”，并回归首页轮播和详情页 | 首页顶部 banner 先读 `index_top_banner`，无数据才兼容旧 `index_banner`；首页核心资源位缺失、广告详情接口失败或无详情数据均进入异常态，不回退旧本地内容 |
| `BE-2026-06-08-mini-program-slot-ads` | 首页楼层“查看全部”已改为携带资源位编码，列表页按资源位直查真实广告列表 | `src/core/services/mini-program-ad.ts`、`src/pages/home/index.tsx`、`src/pkg-ticket/pages/activity-list/index.tsx`、`src/pkg-ticket/pages/park-list/index.tsx`、`src/pkg-ticket/services/activity.ts`、`src/pkg-ticket/services/park-list.ts`、`docs/codex/interface-integration.md`、`docs/codex/current-mini-program.md` | 需要微信开发工具确认小程序授权后资源位直查 200；资源位不存在或停用按后端业务错误暴露，不回退旧 mock | `yarn typecheck`、`yarn check:package-boundary`、`git diff --check` 通过 | 后续新增首页楼层列表页时统一传 `slotCode`，不要再从首页聚合结果或本地 mock 派生列表 |
| `BE-2026-06-08-member-auth-status` | 小程序会员授权登录和会员资料链路已按真实接口重接：启动默认 `login -> member/status`，受保护入口只读 MobX 登录态拦截，手机号授权走官方 `code`，资料保存/头像上传/老会员绑定后刷新全局 `memberInfo`，个人信息页新增退出登录 | `src/core/request/index.ts`、`src/core/services/auth.ts`、`src/core/services/bff-api.ts`、`src/core/services/bff-crm-api.ts`、`src/core/store/member-store.ts`、`src/core/store/root-store.ts`、`src/core/components/LoginPopup`、`src/core/wechat/auth.ts`、`src/pkg-member/services/profile.ts`、`src/pkg-member/services/member-code.ts`、`src/pkg-member/services/index.ts`、`src/pkg-member/pages/profile/index.tsx` | 需要 `$hk-api` 合并根总表最近辅助 commit `30d37b1/348945c/29efdf7/5473fe3/da1b529`，并覆盖旧“BFF 授权返回 mobile 即登录”和 CRM P1 路径结论；小程序仍待微信开发工具真实手机号授权验证 | `yarn typecheck` 通过；待微信开发工具验证 `login -> member/status`、未登录缓存态点击受保护入口不重复 login/memberStatus、`getPhoneNumber code -> phone/authorize -> member/status`、资料保存签名和退出登录 | 长期规则候选：小程序页面不要散写会员登录判断，统一读 `rootStore.isLoggedIn/rootStore.memberInfo`；后端身份字段由 BFF token 内部转换，前端不传不读内部会员标识 |
| `BE-2026-06-09-hotel-order-bff` | 酒店分包和订单中心核心链路已按后端最新 BFF 切真实接口；本轮后端辅助 HEAD 为 `3fe0c1a`。酒店首页/详情/确认单/创建订单/支付/订单列表/订单详情不再读取运行时 mock 或本地订单 | `src/core/services/bff-order-api.ts`、`src/pkg-hotel/services/model.ts`、`src/pkg-hotel/services/bff-api.ts`、`src/pkg-hotel/services/index.ts`、`src/pkg-hotel/services/room-detail.ts`、`src/pkg-hotel/services/order-draft.ts`、`src/pkg-hotel/services/checkout.ts`、`src/pkg-hotel/pages/index/index.tsx`、`src/pkg-hotel/pages/room-detail/index.tsx`、`src/pkg-hotel/pages/checkout/index.tsx`、`src/pkg-order/services/model.ts`、`src/pkg-order/services/index.ts`、`src/pkg-order/services/detail.ts`、`src/pkg-order/pages/index/index.tsx`、`src/pkg-order/pages/detail/index.tsx` | 需要 `$hk-api` 合并根总表最近辅助 commit `3fe0c1a`；后续票务/商城交易链路也必须按 ROOT-035 清理各自历史静态降级，不得复用旧订单本地数据 | `yarn typecheck`、`yarn check:package-boundary`、`yarn check:page-convention`、`git diff --check` 通过；待微信开发工具真实酒店数据和支付参数验收 | 统一订单能力以后继续复用 `confirm -> create -> pay`；页面不得自己生成支付成功、订单详情或订单列表假数据 |
| `BE-2026-06-16-coupon-bff-required` | 小程序优惠券 BFF 必须接口清单已迁入 admin 指定目录；2026-06-18 12:36 复拉后端 `origin/uat@e3138d9`，本次仅影响 order-service 深大默认场所/仓库配置，不改变券或商城 BFF 契约；源码已补 CRM 券实例与 promotion 会员券资产同源同步、`available` 商品/SKU/日期过滤、订单 confirm / promotion quote 候选目标校验、退款返还幂等、商城购物车 BFF 源码，以及商城展示/购物车读接口空态 200；当前小程序按新版字段适配我的券、K 币兑换注释、门票/酒店/商城可用券、商城展示、商城购物车和商城确认单真实 BFF | `docs/codex/apimust/README.md`、`docs/codex/interface-integration.md`、`docs/codex/current-mini-program.md`、`src/core/services/bff-coupon-api.ts`、`src/core/services/bff-api.ts`、`src/core/services/bff-mall-api.ts`、`src/pkg-member/services/coupon-center.ts`、`src/pkg-member/services/coupons.ts`、`src/pkg-member/services/exchange.ts`、`src/pkg-ticket/services/checkout.ts`、`src/pkg-hotel/services/checkout.ts`、`src/pkg-order/services/checkout.ts`、`src/pkg-order/pages/checkout/index.tsx`、`src/pkg-mall/services/cart.ts`、`src/pkg-mall/pages/cart/index.tsx`、`src/core/components/status/StatusException.tsx`；后端必补文档维护在 `admin-frontend/docs/codex/admin-api-requirements/mp-bff-requirements/` | 仍需后端或运营提供目标会员/券号/K 币兑换样本做严格探针，补齐管理端券模板 `target_ids` 同步，并用真实已核销券验收退款返还状态一致；商城读接口空态已通，确认单已补选券、重新确认和创建订单传券，但还需后台配置真实商品后验收商品详情、加购、购物车写入、统一订单创建支付与用券；管理后台缺口继续只在 `admin-frontend/docs/codex/admin-api-requirements/coupon-service.md` 维护；餐饮券闭环仍需订单 BFF 完成后再宣称 | `yarn typecheck`、`yarn check:page-convention`、`yarn check:package-boundary`、`git diff --check` 通过；只读券探针 `coupon-after-e3138d9-01..04` 四个接口 200 但当前会员无券、无券包、K 币余额 0；商城探针 `mall-after-e3138d9-01..07` 全部 200 空结构；Computer Use 两次返回 `codex app-server exited before returning a response`，微信开发工具商城确认单验收未能确认 | 以后新增小程序路由或接口时同步更新 `admin-frontend/docs/codex/admin-api-requirements/mp-bff-requirements/route-api-map.md`；后端更新后需重新核验 Feishu 主文档、OpenAPI、release、Controller、DTO、Service、Mapper |
| `BE-2026-06-19-coupon-resume` | 本轮续跑优惠券闭环：后端 `origin/uat@f56a059` 已复核，`212a323/839f4a5` 回填 K 币兑换商品类型收紧 UAT 文档，`54fcd24/6e3623a` 新增折扣券模板同源字段 `discountPercent/maxDiscountCent`，`248198f/f56a059` 新增多业态/多场景券同源 `sceneType=ALL`；`a0341e2` 已补 promotion 历史 CRM 会员券回填到同源资产，`3cdc8b4/7eb18be` 已关闭发券任务 READY 假闭环；远端 admin `feature/admin-business-platform@bb9ab2d` 已包含小程序 BFF 全量接口盘点、全模块接口设计巡检、券运行态缺口、门票预定接口形态规范和本轮券复核；严格券探针仍无法证明后台发券或 K 币兑换券闭环，且登录态 CRM 展示入口动作字段仍为空；小程序已修正领券中心 K 币 tab 数据源为 `crm/entries/exchanges`，并承接折扣券券面展示 | `src/core/services/bff-coupon-api.ts`、`src/pkg-member/services/coupons.ts`、`src/pkg-member/services/coupon-center.ts`、`src/pkg-ticket/services/checkout.ts`、`src/pkg-hotel/services/checkout.ts`、`src/pkg-order/services/checkout.ts`、`docs/codex/interface-integration.md`、`docs/codex/current-mini-program.md`；后端必补文档维护在 `admin-frontend/docs/codex/admin-api-requirements/mp-bff-requirements/` | 后端必须补 UAT CRM 动作字段配置或数据迁移、稳定 4xx 业务错误、目标 `couponNo`、可领取 `templateNo`、可兑换 `itemNo` 和足够 K 币余额样本；没有样本前只能记录阻塞，不改小程序生产代码造成功 | `coupon-after-212a323-20260619-01..04` 四个读接口 200 但均为空，目标券 `CP1781194544162039A54BA` 不在我的券和可用券；`crm-action-fields-after-b34aa63-03..08` CRM 展示/详情 200 但动作字段全为 `null`；本轮需补跑 `yarn typecheck`、`yarn check:package-boundary`、`yarn check:page-convention`、`git diff --check` | 后端更新后需重新核验 Feishu 主文档、OpenAPI、release、Controller、DTO、Service、Mapper；若找到已有接口但不满足，继续在 admin `mp-bff-requirements/` 写“基于现有接口改造” |

## 文档瘦身规则

- 本文件只保留小程序侧字段映射和联调进度，不复制完整后端接口示例。
- 字段映射只记录 service/adapter 需要的关键字段族，完整 DTO 以 Feishu 主文档、OpenAPI 或 `backend-server/` 后续变更记录为准；commit 只作为批次和差异辅助。
- 页面设计文档只引用本文件的“页面/能力”和接口名，不重复维护字段表。
- `docs/codex/current-mini-program.md` 只保留当前有效接口摘要，不复制本文件进度表。
- 本文件按检查点更新，不记录探索过程；当前 active 批次和最近 1 个 completed 批次优先保留，稳定接口可压缩为摘要。

## 验证清单

- `yarn typecheck`
- `yarn check:package-boundary`
- 按触达页面决定是否运行 `yarn check:page-convention`
- 微信开发工具网络面板确认登录、普通登录态 GET、签名写接口的 header 和响应
- 微信开发工具确认页面异常态、空态和阻断态不出现内部文案；真实接口链路不得回退旧 mock 或静态业务数据
